run:
  timeout: 5m
  skip-dirs:
    - openapi
    - example

linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - typecheck

issues:
  exclude-dirs:
    - openapi
  exclude-rules:
    - path: openapi/
      linters:
        - gofmt
        - errcheck
        - gosimple
        - govet
        - ineffassign
        - staticcheck
        - typecheck
        - unused
